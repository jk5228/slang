(
	( def fib (n)
		(
			( if ( < n 2 )
				(
					( return 1 )
				)
				(
					( return ( + ( fib( ( - n 2 ) ) )
											 ( fib( ( - n 1 ) ) ) ) )
				)
			)
		)
	)
	( for ( i in ( range(0, 10) ) )
		(
			( print( ( fib(i) ) ) )
		)
	)
)